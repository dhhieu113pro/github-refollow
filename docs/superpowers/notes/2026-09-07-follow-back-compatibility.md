# Compatibility notes

Preserve the current .NET 10 project, Monday 09:00 Asia/Ho_Chi_Minh schedule, existing environment-variable names, localhost binding, and persistent /data volume. Existing following.json and pending.json files are retained, but old account lists are not authorization to follow users who are no longer current followers. Update status and dashboard counts to reflect current followers and the missing follow-back set.
