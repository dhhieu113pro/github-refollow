# Implementation state

The approved source change has not yet been applied. The local container has no .NET SDK and cannot resolve github.com, so verification must use the repository's GitHub Actions. Create an isolated branch from the original source commit, implement the tests and code through GitHub, and validate the PR using CI. Do not perform any live account mutations.
