# Code work

The source fix is approved and ready to implement. Use the GitHub connector to create the feature branch, add regression tests and production changes, and validate with CI. The change must fetch current followers, compare against following, and issue PUT-only follow requests for missing accounts. Existing follows and unrelated accounts must remain untouched.
