# Final checklist

The implementation must fetch current followers and following, compute the missing set, preserve dry-run, use only PUT for missing accounts, reconcile pending entries safely, update status and dashboard, and include regression tests. Before completion, run the full test suite and container CI, review the diff, and open a PR. No live follow/unfollow operations are part of this development work.
