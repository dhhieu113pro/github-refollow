# Follow-back acceptance

The user approved replacing the current destructive re-follow behavior with follow-back. In the example, 17 followers include 14 already followed and three missing. A live run follows only the three missing accounts. The actual account data determines counts; there is no hard-coded target count. Existing follows must remain untouched, and dry-run mode remains the default.
